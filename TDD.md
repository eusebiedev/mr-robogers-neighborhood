```
Describe: numberUp()

Test: "It should return a list of numbers from 0 to the users inputted number'" 
Code: numberUp(3)
Expected Output: "0,1,2,3"

Test: "It should replace digits that contain a 1 with "Beep!", up to the users inputted number"
Code: numberUp(1)
Expected Output: "0, Beep!"

Test: "It should replace digits that contain a 2, with "Boop!", up to the users inputted number"
Code: numberUp(2)
Expected Output: "0, Beep!, Boop!"

Test: "It should replace digits that contain a 3, with "Won't you be my neighbor?", up to the users inputted number"
Code: numberUp(3)
Expected Output: "0, Beep!, Boop!, Won't you be my neighbor?"

Test: "It should return all substitutions below counting up to and list the users inputted number, and display results in reverse order"
Code: numberUp(4)
Expected Output: "4, Won't you be my neighbor?, Boop!, Beep!, 0"

```
